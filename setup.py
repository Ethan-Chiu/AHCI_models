from setuptools import setup, find_packages

setup(
    name='hmipt',
    version='0.1',
    packages=find_packages(),
    description='Hand motion intention prediction',
    author='Ethan Chiu',
    author_email='ethanchiu0608@gmail.com',
    install_requires=[
        # List dependencies here if your package requires any
    ],
)
